# Claude Plugin Directory

This folder is a local template mirror of the public Claude Plugins directory.

The source catalog is `../claude-plugin-directory.config.json`. Each folder in `plugins/` mirrors one public directory entry with metadata and capability hints.

These folders are disabled by default. Install and authenticate each plugin in the target environment before use; never store tokens, OAuth grants, or API keys here.
